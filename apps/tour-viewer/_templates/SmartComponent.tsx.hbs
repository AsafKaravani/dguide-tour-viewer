import React from 'react';

type {{pascalCase name}}Props = {
	children?: React.ReactNode;
};

export const {{pascalCase name}}: React.FC =	React.memo<{{pascalCase name}}Props>(
	function {{pascalCase name}}(_p) {
		return (
			<div>
				<p>{{sentenceCase name}} works.</p>
				{_p.children}
			</div>
		);
	});
